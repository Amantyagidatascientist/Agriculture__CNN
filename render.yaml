services:
  - type: web
    name: tomato-leaf-api
    env: python
    buildCommand: ""
    startCommand: uvicorn API.main:app --host 0.0.0.0 --port 10000
    runtime: python
    plan: free
    region: oregon
